renew:
  name: Renew Certificate
  description: Triggers a renewal of a specific certificate managed by ACME Entities.
  fields:
    cert_name:
      name: Certificate Name
      description: The name of the certificate to renew (e.g. example.com).
      required: true
      example: "example.com"
      selector:
        text:
